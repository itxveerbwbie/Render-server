services:
  - type: web
    name: fb-e2ee-sender
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: "python render-server.py"
    plan: free
    envVars:
      - key: APPSTATE_B64
        sync: false     # this tells Render to read value from dashboard env var
